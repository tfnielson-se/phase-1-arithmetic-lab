const num1 = 31;
const num2 = 2;
const num3 = 6;
const num4 = 2;
const array1 = [1, 2, 3, 10, 20];

function multiply () {
    let multi = (num1 * num2);
    return multi;
};

multiply;


function random () {
    let randInt = Math.ceil(Math.random() * 10) + 1;
    return randInt
};

function mod () {
    let remain = (num3 - num4)
    return remain
};


function max () {
    maxNum = Math.max(array1)
    return maxNum
};